name: flutter_image_labelling
description: Example of image labeling with Firebase ML on Flutter
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_ml_vision: ^0.9.10
  image_picker: ^0.6.7+21

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true